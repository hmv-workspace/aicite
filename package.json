{
  "name": "aicite-repo",
  "private": true,
  "description": "AiCite monorepo (npx CLI today; uvx/OS packages later).",
  "workspaces": [
    "npx"
  ],
  "scripts": {
    "smoke:npx": "npm --workspace ./npx run smoke",
    "pack:npx": "npm --workspace ./npx pack --dry-run"
  }
}
